# Operational Memory — turtleatlas-w40k-11e

Learned rules and reference data for THIS project. Load when working in this codebase.

Seeded 2026-08-05 from the Aeldari squad-composition session (commits `63741a6` → `6e4558e`).
The point: a fresh session on any machine picks up project knowledge from the repo, not from a dying conversation.

## Files

| File | Contents |
|------|----------|
| `feedback.md` | Project-specific learned rules — conventions, diagnosis approach, process rules |
| `reference.md` | Repo map, commands, data sources, current state, next moves |

## Load rules

1. Read both files before touching `engine/`, `adapter/`, `data/config/`, `scripts/`, or `findings/`.
2. Global turtle rules live in the `turtle-dojo` skill (opencode-turtle-skills repo, external — clone + configure on a new machine). Git discipline, Shredder review, no fabricated numbers, four-tier output. This layer only adds what is true for THIS repo.
3. When a session proves a new lesson, append it to `feedback.md` in the same commit. Never let a lesson live only in a conversation.
4. Mechanics-only: no GW IP, no secrets. Repo is public.
