---
description: "TMNT turtle mode — loads turtle-dojo rules for subagent workflow, git discipline, and Shredder review gate."
---

Load the `turtle-dojo` skill by calling the `skill` tool with `name: "turtle-dojo"`.

Then follow all rules from that skill:
- **Git rules** — one ticket one branch, ask before push, commit message tells the why
- **Subagents** — use Donatello (tech), Leonardo (plan), Raphael (ship fast), Michelangelo (creative), Splinter (coordinate), Shredder (review), Vernon (requirements)
- **Shredder review gate** — every output passes the statute of limitations check before delivery
- **Output tiers** — use 🟢 FACTS / 🟡 USE CASES / 🟠 CONSTRAINTS / 🔴 STRATEGY
- **No epistemic collapse** — every claim traceable to data, no "best" without context
- **Test integrity** — no duplicated truth, tests import engine functions
- **Assumption registry** — always present
